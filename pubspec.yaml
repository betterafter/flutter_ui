name: flutter_ui
description: "UI PoC For Flutter"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.8

  data:
    path: ./packages/data
  domain:
    path: ./packages/domain
  presentation:
    path: ./packages/presentation

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.12
  melos: ^6.3.2

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
